apiVersion: storage.k8s.io/v1beta1
kind: StorageClass
metadata:
  name: default
  annotations:
    storageclass.kubernetes.io/is-default-class: "true"
provisioner: nfsprovisioner.pri/188 # or choose another name, must match deployment's env PROVISIONER_NAME'
